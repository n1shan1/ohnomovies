# # Production Environment Configuration
# spring.profiles.active=prod

# # Database - Production settings
# spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:ohnomovies}?useSSL=true&allowPublicKeyRetrieval=false&serverTimezone=UTC&charset=utf8mb4&useUnicode=true
# spring.datasource.hikari.maximum-pool-size=50
# spring.datasource.hikari.minimum-idle=10

# # Security - Strict settings
# server.servlet.session.cookie.secure=true
# server.ssl.enabled=true

# # Logging - Production level (reduced verbosity)
# logging.level.com.ohnomovies.backend=WARN
# logging.level.org.springframework=WARN
# logging.level.org.hibernate=WARN

# # Disable H2 console in production
# spring.h2.console.enabled=false

# # Disable SQL logging in production
# spring.jpa.show-sql=false

# # Management endpoints - restricted
# management.endpoints.web.exposure.include=health
# management.endpoint.health.show-details=never

# # Error handling - no stack traces in production
# server.error.include-stacktrace=never
# server.error.include-message=always
# server.error.include-binding-errors=never